#Задача «Следующее и предыдущее»
n = int(input())
nextNum = n+1
prevNum = n-1

print("The next number for the number "+str(n)+" is "+str(nextNum)+".")
print("The previous number for the number "+str(n)+" is "+str(prevNum)+".")
